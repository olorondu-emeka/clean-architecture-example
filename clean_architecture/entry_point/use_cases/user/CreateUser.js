const SuccessResponse = require('../../definitions/SuccessResponse');
const ErrorResponse = require('../../definitions/ErrorResponse');

class CreateUser {
  constructor() {}

  execute({ lastName, firstName, email, password }) {
    try {
    } catch (error) {}
  }
}

module.exports = CreateUser;
